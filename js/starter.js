window.onload = function () {
	var line = document.getElementById('fh5co-header');
	const page_height = window.outerHeight * .5
	if (line) {
		line.style.height = page_height + 'px';
	}
}

window.onload = function () {
	var line = document.getElementById('fh5co-header');
	const page_height = window.outerHeight * .6
	if (line) {
		line.style.height = page_height + 'px';
	}
}